import { {{pascalCase component}} } from './{{pascalCase component}}';

export default {
  title: 'components/{{pascalCase component}}',
  component: {{pascalCase component}},
};

const Template = (args) => <{{pascalCase component}} {...args} />;

export const Primary = Template.bind({});

Primary.args = {
};
